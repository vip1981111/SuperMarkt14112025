# دليل حذف القوائم 🗑️

## الميزات المضافة ✨

تم إضافة خاصية **مسح القوائم** بطرق متعددة:

---

## 1️⃣ الحذف بالسحب (Swipe to Delete)

### للقوائم النشطة:

**اسحب من اليمين إلى اليسار** على أي قائمة لإظهار الخيارات:

- 🗑️ **حذف**: يحذف القائمة نهائياً
- 📦 **أرشفة**: ينقل القائمة إلى الأرشيف
- 📄 **نسخ**: ينشئ نسخة من القائمة (بالسحب من اليسار)

---

### للقوائم المؤرشفة:

**اسحب من اليمين إلى اليسار** على القائمة المؤرشفة:

- 🗑️ **حذف**: يحذف القائمة نهائياً
- ↩️ **استعادة**: يعيد القائمة للقوائم النشطة

---

## 2️⃣ مميزات الحذف

### ✅ حذف مباشر
- اسحب بالكامل للحذف السريع (Full Swipe)
- أو اسحب جزئياً لإظهار الخيارات

### ✅ تأكيد الحذف (اختياري)
- يمكن إضافة رسالة تأكيد قبل الحذف
- حماية من الحذف الخاطئ

### ✅ حذف مع Animation
- جميع عمليات الحذف مصحوبة بـ Animation سلسة

---

## 3️⃣ خيارات إضافية

### الأرشفة:
بدلاً من الحذف، يمكنك **أرشفة** القائمة:
- القائمة تبقى محفوظة
- يمكن استعادتها لاحقاً
- لا تظهر في القوائم النشطة

### النسخ:
اسحب من **اليسار إلى اليمين** لنسخ القائمة:
- تنشئ نسخة جديدة
- جميع العناصر تُنسخ
- حالة الشراء تُعاد للبداية

---

## 4️⃣ كيفية الاستخدام

### حذف قائمة نشطة:
```
1. اذهب إلى "القوائم"
2. اسحب القائمة من اليمين ← اليسار
3. اضغط "حذف" 🗑️
4. القائمة تُحذف فوراً
```

### أرشفة قائمة:
```
1. اسحب القائمة من اليمين ← اليسار
2. اضغط "أرشفة" 📦
3. القائمة تنتقل للأرشيف
```

### استعادة قائمة مؤرشفة:
```
1. افتح "القوائم المؤرشفة"
2. اسحب القائمة من اليمين ← اليسار
3. اضغط "استعادة" ↩️
4. القائمة ترجع للقوائم النشطة
```

### نسخ قائمة:
```
1. اسحب القائمة من اليسار ← اليمين
2. اضغط "نسخ" 📄
3. تظهر نسخة جديدة بنفس العناصر
```

---

## 5️⃣ الكود المضاف

### في ListsView.swift:

تم إضافة:
- `.swipeActions(edge: .trailing)` للحذف والأرشفة
- `.swipeActions(edge: .leading)` للنسخ
- `allowsFullSwipe: true` للحذف السريع
- متغيرات لتأكيد الحذف (اختياري)

### الدوال المستخدمة:

من `ShoppingListViewModel`:
- ✅ `deleteList(_ list:)` - حذف القائمة
- ✅ `archiveList(_ list:)` - أرشفة/استعادة
- ✅ `duplicateList(_ list:)` - نسخ القائمة

---

## 6️⃣ تخصيصات إضافية (اختيارية)

### إضافة تأكيد الحذف:

الكود الحالي يحذف مباشرة. إذا أردت إضافة تأكيد:

```swift
// في ListsView
@State private var showingDeleteConfirmation = false
@State private var listToDelete: ShoppingList?

// في swipeActions
Button(role: .destructive) {
    listToDelete = list
    showingDeleteConfirmation = true
} label: {
    Label("حذف", systemImage: "trash")
}

// بعد .alert
.confirmationDialog("هل أنت متأكد؟", isPresented: $showingDeleteConfirmation) {
    Button("حذف نهائياً", role: .destructive) {
        if let list = listToDelete {
            viewModel.deleteList(list)
        }
    }
}
```

---

## 7️⃣ الألوان والأيقونات

### الألوان المستخدمة:
- 🔴 **أحمر**: الحذف (destructive)
- 🟠 **برتقالي**: الأرشفة
- 🟢 **أخضر**: الاستعادة
- 🔵 **أزرق**: النسخ

### الأيقونات:
- `trash`: الحذف
- `archivebox`: الأرشفة
- `arrow.uturn.backward`: الاستعادة
- `doc.on.doc`: النسخ

---

## 8️⃣ ملاحظات مهمة ⚠️

### الحذف نهائي:
- ⚠️ عند حذف قائمة، لا يمكن استعادتها
- استخدم **الأرشفة** إذا كنت غير متأكد
- الأرشفة تحفظ القائمة للمستقبل

### الأداء:
- ✅ جميع العمليات محفوظة في UserDefaults
- ✅ يتم الحفظ التلقائي بعد كل عملية
- ✅ Animations سلسة مع `withAnimation`

---

## 9️⃣ اختبار الميزة

### للتأكد من عمل الميزة:

1. ✅ أنشئ قائمة جديدة
2. ✅ اسحبها من اليمين لليسار
3. ✅ يجب أن ترى أزرار: حذف، أرشفة
4. ✅ اضغط حذف → القائمة تختفي
5. ✅ جرب الأرشفة والاستعادة
6. ✅ جرب النسخ (سحب من اليسار)

---

## 🎉 الخلاصة

الآن يمكنك:
- ✅ حذف القوائم بسهولة
- ✅ أرشفة القوائم بدلاً من الحذف
- ✅ استعادة القوائم المؤرشفة
- ✅ نسخ القوائم
- ✅ حذف سريع بالسحب الكامل

---

**تم إضافة جميع الميزات بنجاح! 🚀**
