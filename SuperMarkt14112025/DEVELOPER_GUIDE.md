# دليل المطور - تطبيق قائمة التسوق الذكية

## البنية الهيكلية للمشروع

```
SuperMarkt14112025/
├── Models/
│   ├── ShoppingItem.swift          # نموذج المنتج
│   └── ShoppingList.swift          # نموذج القائمة
│
├── ViewModels/
│   ├── ShoppingListViewModel.swift # إدارة القوائم والمنتجات
│   └── CameraViewModel.swift      # إدارة الكاميرا والتعرف على الأسعار
│
├── Views/
│   ├── ContentView.swift          # الواجهة الرئيسية (TabView)
│   ├── ListsView.swift            # عرض جميع القوائم
│   ├── ShoppingListDetailView.swift # تفاصيل القائمة
│   ├── AddItemView.swift          # إضافة/تعديل منتج
│   ├── CameraView.swift           # ميزة تسوق بالمبلغ
│   ├── PriceHistoryView.swift     # عرض قائمة الأسعار المضافة
│   ├── StatisticsView.swift       # إحصائيات القائمة
│   ├── AllStatisticsView.swift    # الإحصائيات العامة
│   ├── SettingsView.swift         # الإعدادات
│   └── OnboardingView.swift       # شاشة التعريف
│
├── Utilities/
│   └── Extensions.swift           # ملحقات مفيدة
│
├── Widget/
│   └── ShoppingListWidget.swift   # Widget للشاشة الرئيسية
│
└── Resources/
    ├── Info.plist                 # إعدادات التطبيق والأذونات
    └── README.md                  # وثائق المشروع

```

## الميزات المطلوب تفعيلها في Xcode

### 1. أذونات الكاميرا
تأكد من إضافة الأذونات التالية في Info.plist:
- `NSCameraUsageDescription`: "نحتاج للوصول إلى الكاميرا لمسح الأسعار وإضافتها تلقائياً"

### 2. الإطارات المطلوبة (Frameworks)
- SwiftUI
- AVFoundation (للكاميرا)
- Vision (للتعرف على النصوص)
- Charts (للرسوم البيانية)
- WidgetKit (للWidget - اختياري)

### 3. الإمكانيات (Capabilities)
لا توجد إمكانيات إضافية مطلوبة حالياً، ولكن يمكن إضافة:
- iCloud (للمزامنة في المستقبل)
- App Groups (للWidget)

## شرح المكونات الرئيسية

### Models

#### ShoppingItem
```swift
struct ShoppingItem: Identifiable, Codable {
    var id: UUID
    var name: String
    var quantity: Int
    var price: Double
    var category: Category
    var isPurchased: Bool
    var notes: String
    var addedDate: Date
}
```

#### ShoppingList
```swift
struct ShoppingList: Identifiable, Codable {
    var id: UUID
    var name: String
    var items: [ShoppingItem]
    var createdDate: Date
    var isArchived: Bool
}
```

### ViewModels

#### ShoppingListViewModel
المسؤول عن:
- إدارة القوائم (إنشاء، تعديل، حذف)
- إدارة المنتجات (إضافة، تعديل، حذف)
- الحفظ والتحميل من UserDefaults
- البحث والتصفية
- الإحصائيات

#### CameraViewModel
المسؤول عن:
- إدارة جلسة الكاميرا (AVCaptureSession)
- التعرف على النصوص باستخدام Vision Framework
- استخلاص الأرقام من النصوص
- إدارة قائمة الأسعار المضافة

### Views الرئيسية

#### CameraView - ميزة "تسوق بالمبلغ"
هذه هي الميزة الأساسية المطلوبة:
- تفتح الكاميرا مباشرة
- تستخدم Vision Framework للتعرف على الأرقام
- تعرض الرقم المكتشف بشكل فوري
- زر لإضافة الرقم للمجموع
- زر للإدخال اليدوي
- زر لعرض قائمة الأسعار
- حساب المجموع الكلي تلقائياً

## خطوات تشغيل المشروع

### 1. المتطلبات الأساسية
- Xcode 15.0 أو أحدث
- iOS 17.0 أو أحدث
- Swift 5.9 أو أحدث

### 2. إعداد المشروع
```bash
# 1. افتح المشروع في Xcode
open SuperMarkt14112025.xcodeproj

# 2. اختر جهاز حقيقي (الكاميرا لا تعمل على المحاكي)

# 3. اضغط على Run (⌘R)
```

### 3. اختبار ميزة الكاميرا
للاختبار على جهاز حقيقي:
1. افتح التطبيق
2. أنشئ قائمة جديدة أو افتح قائمة موجودة
3. اضغط على زر "تسوق بالمبلغ" 📸
4. وجه الكاميرا نحو أي رقم (سعر على منتج، إيصال، إلخ)
5. سيتم التعرف على الرقم تلقائياً
6. اضغط "إضافة" لإضافته للمجموع

## التخصيص والتطوير

### إضافة فئة جديدة
في `ShoppingItem.swift`:
```swift
enum Category: String, Codable, CaseIterable {
    case newCategory = "اسم الفئة"
    
    var icon: String {
        case .newCategory: return "🆕"
    }
    
    var color: Color {
        case .newCategory: return .indigo
    }
}
```

### تحسين دقة التعرف على الأسعار
في `CameraViewModel.swift`:
```swift
private func extractNumbers(from text: String) -> [Double] {
    // أضف نمط جديد للتعرف
    let patterns = [
        #"(\d+[.,]\d{1,2})"#,  // موجود
        #"SR\s*(\d+)"#,         // جديد: SR 10
        // أضف أنماط أخرى
    ]
}
```

### إضافة عملة جديدة
في `SettingsView.swift`:
```swift
Picker(selection: $preferredCurrency) {
    Text("ريال سعودي (SAR)").tag("SAR")
    Text("عملة جديدة").tag("NEW")
}
```

## حل المشاكل الشائعة

### الكاميرا لا تعمل
- تأكد من إضافة `NSCameraUsageDescription` في Info.plist
- استخدم جهاز حقيقي (المحاكي لا يدعم الكاميرا)
- تأكد من منح التطبيق أذونات الكاميرا في الإعدادات

### التعرف على الأرقام غير دقيق
- تأكد من إضاءة جيدة
- ثبت الهاتف لتقليل الاهتزاز
- استخدم الإدخال اليدوي للأرقام الغامضة
- حسّن أنماط البحث في `extractNumbers`

### البيانات لا تُحفظ
- تحقق من `saveLists()` في ViewModel
- تأكد من استدعاء `saveLists()` بعد كل تغيير
- استخدم UserDefaults debugger لفحص البيانات

## إضافات مستقبلية مقترحة

### 1. المزامنة السحابية
```swift
// استخدم CloudKit أو Core Data مع iCloud
import CloudKit
```

### 2. إشعارات التذكير
```swift
// استخدم UserNotifications
import UserNotifications
```

### 3. مشاركة القوائم
```swift
// استخدم ShareLink في SwiftUI
ShareLink(item: list)
```

### 4. تصدير/استيراد
```swift
// تصدير كـ JSON أو CSV
func exportList() -> Data {
    try JSONEncoder().encode(list)
}
```

### 5. دعم Siri Shortcuts
```swift
// استخدم App Intents
import AppIntents
```

## أفضل الممارسات

### 1. إدارة الذاكرة
- استخدم `@MainActor` للعمليات على الواجهة
- استخدم `weak self` في الـ closures
- نظف الموارد في `onDisappear`

### 2. الأداء
- استخدم `LazyVStack` للقوائم الطويلة
- قلل عمليات الحفظ غير الضرورية
- استخدم Debouncing للبحث

### 3. تجربة المستخدم
- أضف Haptic Feedback للتفاعلات
- استخدم الرسوم المتحركة بحذر
- وفر حالات فارغة واضحة

### 4. الأمان
- لا تحفظ بيانات حساسة في UserDefaults
- استخدم Keychain للبيانات الحساسة
- تحقق من الأذونات قبل استخدام الكاميرا

## الاختبار

### اختبار الوحدات
```swift
import Testing

@Test("إضافة منتج")
func testAddItem() {
    let viewModel = ShoppingListViewModel()
    let list = ShoppingList(name: "Test", items: [])
    let item = ShoppingItem(name: "Test", quantity: 1, price: 10, category: .other)
    
    viewModel.addItem(to: list, item: item)
    
    #expect(list.items.count == 1)
}
```

### اختبار الواجهة
```swift
import XCTest

class UITests: XCTestCase {
    func testCameraView() {
        let app = XCUIApplication()
        app.launch()
        
        app.buttons["تسوق بالمبلغ"].tap()
        // المزيد من الاختبارات
    }
}
```

## الدعم والمساهمة

للمساهمة في المشروع:
1. Fork المشروع
2. أنشئ branch جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للـ branch (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

تم التحديث: 14 نوفمبر 2025
